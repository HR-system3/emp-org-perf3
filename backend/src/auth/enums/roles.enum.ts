// Role enum for leaves module - maps to SystemRole from employee-profile
export enum Role {
  EMPLOYEE = 'department employee',
  MANAGER = 'department head',
  HR = 'HR Manager',
  HR_EMPLOYEE = 'HR Employee',
  ADMIN = 'System Admin',
}

